# estomato-api

Instalar:
  `npm install`

Buildear la bbdd:
  Crear el modelo:
    `npm run db:create`
  Seedear la base:  
    `npm run db:seed`

Correr:
  Normal:
    `npm start`
  Nodemon:
    `npm run dev`
